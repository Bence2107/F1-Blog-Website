import { CapitalizeFirstPipe } from './capitalizefirstpipe.pipe';

describe('CapitalizefirstpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
